# Production requirements for ML Extractor
# Core FastAPI dependencies
# Minimal production requirements for ML Extractor on shared hosting (cPanel)
# Trimmed to avoid very large ML packages and optional AI SDKs.
# If you need spaCy, OpenAI or other heavy packages, install them separately.

fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
aiofiles==23.2.1

# Security / auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# File processing
pandas==2.0.3
openpyxl==3.1.2
xlrd==2.0.1
python-docx==1.1.0
PyPDF2==3.0.1
PyYAML==6.0.1
flask-cors==4.0.0

# Database / migrations
SQLAlchemy==2.0.23
alembic==1.13.1

# ASGI sync helper
asgiref==3.7.2

# Notes:
# - spaCy and OpenAI SDKs are intentionally omitted to keep installation small on cPanel.
# - If your production environment supports them, add spaCy, openai, transformers, etc. as needed.
