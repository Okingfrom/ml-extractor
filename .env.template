# .env.template - copy to .env and fill values
# Example values — change before deploying
SECRET_KEY=replace-with-a-secure-random-string
FLASK_ENV=production
DATABASE_URL=sqlite:///users.db
MAIL_SERVER=
MAIL_PORT=587
MAIL_USERNAME=
MAIL_PASSWORD=
ADMIN_EMAIL=admin@example.com

# If you want to run behind a reverse proxy or set different host/port
HOST=127.0.0.1
PORT=8000

# Optional: Git repository URL for auto-deploy scripts
REPO_URL=git@github.com:Okingfrom/ml-extractor.git
# .env.template - copy to .env and fill values
# Example values — change before deploying
SECRET_KEY=replace-with-a-secure-random-string
FLASK_ENV=production
DATABASE_URL=sqlite:///users.db
MAIL_SERVER=
MAIL_PORT=587
MAIL_USERNAME=
MAIL_PASSWORD=
ADMIN_EMAIL=admin@example.com

# If you want to run behind a reverse proxy or set different host/port
HOST=127.0.0.1
PORT=8000

# Optional: Git repository URL for auto-deploy scripts
REPO_URL=git@github.com:Okingfrom/ml-extractor.git
